# Leave as /api for local development (Vite proxy handles it).
# For a deployed frontend, set this to the public backend API base, e.g.:
# VITE_API_BASE=https://your-backend.example.com/api
VITE_API_BASE=/api
